#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
  *main-random number is positive, negative or zero
  *
  *Return:0
  */

int main()
{
	char ch;
	for (ch = 'a'; ch <= 'z'; ch++)
		putchar (ch);
	return (0);
}
	
