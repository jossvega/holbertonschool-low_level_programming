#include "holberton.h"

/**
 * mul - function that multiplies two integers.
 * @a: first input value to check
 * @b: Sec input value to check
 * Return: int.
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	return (0);
}
